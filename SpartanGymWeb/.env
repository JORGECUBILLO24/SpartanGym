# URL de la API en produccion (Render, conectada a Neon).
# El web usa esta URL para todas las llamadas (services/api.js).
# Para desarrollo local contra la API en tu PC, cambiar a: http://localhost:8080/api
VITE_API_BASE_URL=https://spartangym-api.onrender.com/api
